CONSTANTS
    NULL = NULL

SPECIFICATION Spec

\* Deadlock checking disabled: the HAMT cancel_state leak (🎯T14)
\* causes a terminal state when the diagnostic invariants are disabled.
CHECK_DEADLOCK FALSE

INVARIANTS
    TypeOK
    NoLeaks
    \* HamtClean — disabled: documents known HAMT cancel_state leak (🎯T14)
    \* LocalRestored — disabled: same HAMT lifecycle issue (🎯T14)
