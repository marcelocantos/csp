SPECIFICATION Spec

CONSTANT
    Imps = {src, sink, tick}
\* Strategy explores design space — the implementation uses a hybrid
\* (atomic exchange in make_runnable + resume fallback) that doesn't
\* map to a single strategy.  Spec is retained for documentation;
\* CI verifies invariants under "match" (closest to make_runnable).
    Strategy = "match"

CHECK_DEADLOCK FALSE

INVARIANT
    TypeOK
    NonNegativeActive
\* HookOnlyWhenQuiescent and HookEventuallyFires depend on strategy
\* choice; the hybrid implementation satisfies both in practice but
\* no single TLA+ strategy models the hybrid correctly.
\*    HookOnlyWhenQuiescent
\*
\* PROPERTY
\*    HookEventuallyFires
